# Frontend — rename this file to .env
# In development, leave this as "/api" — Vite will proxy it to the Express server (see vite.config.ts).
# In production (e.g. Vercel), set this to the full backend URL, e.g. https://api.example.com/api
VITE_API_URL=/api
